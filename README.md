# Grade 4 CogAT Practice

Static GitHub Pages app for fourth-grade CogAT practice.

## Publish on GitHub Pages

Use the repository root as the Pages source. No build step is needed.

## History JSON

The app stores correct/missed history in `localStorage`. Use **JSON history → Export** and **Import** to move that history between browsers or devices.
